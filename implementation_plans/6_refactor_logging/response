Created response
Viewed etl_flow.py:1-168
Viewed ingest_data.py:1-67
Edited etl_flow.py
Edited ingest_data.py
Ran command: `git add orchestration/flows/etl_flow.py scripts/ingest_data.py`
Ran command: `git commit -m "refactor: replace print statements with professional logging (Prefect & Standard)

Inside Prefect tasks and flows, utilized prefect.get_run_logger.
For logic outside prefect-decorated functions like the pandas ingestion script,
implemented the standard python logging module configured for the desired levels.
Outputs are categorized correctly as DEBUG, INFO, WARNING, and ERROR.

Co-authored-by: dicrojasch <dicrojasch@unal.edu.co>
Co-authored-by: Antigravity <ai-agent@google.com>"`
Ran command: `git log -n 1`

I have refactored the files to replace the existing `print()` statements with structured, professional logging. 

Here are the key changes made based on your requirements:

1. **Prefect Logging in Orchestrator**: Inside `etl_flow.py`, all `@task` and `@flow` blocks now explicitly import and use `prefect.get_run_logger()`. Output messages map carefully to `logger.info`, `logger.warning`, `logger.debug`, and `logger.error` following your severity strategy. (e.g. `logger.debug` is used for deep hash calculation and file skipping details).
2. **Standard Library Fallback in Scripts**: For `scripts/ingest_data.py` (which operates outside of Prefect functions), I imported the `logging` module and configured it to point to `sys.stdout` so that the Prefect parent process can still catch those outputs easily.
3. **Structured Debugging Info**: Rather than printing arrays randomly, detailed pieces like the Data Profile Pandas output are cleanly parsed into a `logger.debug()` message.
4. **Git Standards Compliant Commit**: I committed the code adapting your suggested commit message to perfectly follow the `Conventional Commits` project rule while enforcing the AI trailer signature correctly.

The result of the commit details is:
```text
refactor: replace print statements with professional logging (Prefect & Standard)

Inside Prefect tasks and flows, utilized prefect.get_run_logger.
For logic outside prefect-decorated functions like the pandas ingestion script,
implemented the standard python logging module configured for the desired levels.
Outputs are categorized correctly as DEBUG, INFO, WARNING, and ERROR.

Co-authored-by: dicrojasch <dicrojasch@unal.edu.co>
Co-authored-by: Antigravity <ai-agent@google.com>
```